# Normalize line endings so a checkout on another machine does not show every
# text file as modified. Repo stores LF; working copies get LF.
* text=auto eol=lf

# Never touch these
*.png  binary
*.jpg  binary
*.pdf  binary
*.docx binary
